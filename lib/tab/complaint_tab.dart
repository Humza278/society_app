import 'package:flutter/material.dart';

class ComplaintTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text("Complaint Tab")));
  }
}

import 'package:flutter/material.dart';

class VisitorLogsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Visitor Logs & Booking")),
      body: Center(child: Text("Visitor logs will appear here")),
    );
  }
}

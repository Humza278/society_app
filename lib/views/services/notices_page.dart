import 'package:flutter/material.dart';

class NoticesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Notices & Announcements")),
      body: Center(child: Text("All notices will appear here")),
    );
  }
}
